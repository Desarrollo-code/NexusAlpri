
import type { EnterpriseResource } from '@/types';

// mockCourses has been removed as courses are now fetched from the API
// mockAnnouncements has been removed as announcements are now fetched from the API
// mockResources has been removed as resources are now fetched from the API

// If there are other mock data arrays that are still needed, they would remain here.
// For now, this file might become empty or be removed if no other mocks are used.

export const examplePlaceholder = true; // Placeholder if file becomes empty
