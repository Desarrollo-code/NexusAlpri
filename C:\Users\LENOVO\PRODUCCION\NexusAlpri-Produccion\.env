# Este es tu archivo de entorno local.
# NO subas este archivo a GitHub.

# URL de conexión a tu base de datos MySQL LOCAL
# Reemplaza USER, PASSWORD, HOST, PORT y DATABASE_NAME con los valores de tu base de datos local.
# Ejemplo: DATABASE_URL="mysql://root:mysecretpassword@localhost:3306/nexusdb"
DATABASE_URL="mysql://USER:PASSWORD@HOST:PORT/DATABASE_NAME"

# Clave secreta para firmar los tokens JWT.
# Puedes generar una cadena aleatoria y segura aquí.
# Ejemplo: openssl rand -base64 32
JWT_SECRET="una-clave-muy-segura-y-dificil-de-adivinar-para-desarrollo"

# Clave de API para el servicio de envío de correos (Resend)
# Solo es necesaria si quieres probar el envío de correos en local.
# Puedes obtenerla desde https://resend.com
RESEND_API_KEY="re_123456789"
