# Este archivo contiene las variables de entorno para el desarrollo local.
# NO subas este archivo a GitHub.

# URL de conexión a tu base de datos MySQL local.
# Reemplaza USER, PASSWORD, HOST, PORT y DATABASE_NAME con los de tu base de datos.
# Ejemplo para una base de datos local estándar:
# DATABASE_URL="mysql://root:tu_contraseña@localhost:3306/nexusdb"
DATABASE_URL="mysql://USER:PASSWORD@HOST:PORT/DATABASE_NAME"

# Clave secreta para firmar los tokens de sesión (JWT).
# Puedes generar una cadena larga y aleatoria aquí.
JWT_SECRET="una-clave-secreta-muy-larga-y-dificil-de-adivinar-para-proteger-sesiones"

# Clave de API para el servicio de envío de correos (Resend).
# Consigue esta clave desde tu cuenta de Resend.com
RESEND_API_KEY="re_xxxxxxxxxxxxxxxx"
